# Copy selected text, or if nothing is selected, copy the last command's output.
map ctrl+shift+y copy_selection_or_last_command_output

# Copy output from every command in the most recent pasted command block.
map ctrl+shift+o kitten copy_last_command_block.py
